<Window x:Class="WpfApp1.WindowCost"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="WindowCost" Height="450" Width="800">
    <Grid>
        <Label HorizontalAlignment="Center" FontSize="50">Costume</Label>
        <StackPanel>
            <Button x:Name="Bttn_ShowCost" Width="100" Margin="0 90 0 0" Background="LightBlue" Click="Bttn_ShowCost_Click" >Show Costume</Button>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="117,120,132,62">
            <TextBox x:Name="Cstm" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Visible">
                
            </TextBox>
        </StackPanel>
    </Grid>
</Window>
